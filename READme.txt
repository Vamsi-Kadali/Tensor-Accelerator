17/3/26
-Prithvi:- 1.Created a rough file including top module, font module and vga controller module which should be able to display output on vga in the form of numbers. 
           2.'vga_numbers.v' isnt final but we need to be able to integrate such a module into our tensor arithmetic code to get tensor output on vga.
           3. There are 2 possibilities with our approach, we either use numbers(for which i have just given the code) or we can display the numbers via colour and make it application(image) based.
